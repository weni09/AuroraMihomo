## Instructions

- Type refs and reactive objects explicitly.
- Use interfaces for structured data.
- Use return types for composables.

### Example

```ts
import { ref } from 'vue'

const count = ref<number>(0)
```

Reference: https://cn.vuejs.org/guide/typescript/composition-api.html
