# 运维实验脚本（非产品）

`patch_129_*.py` 等脚本仅用于特定主机（如 192.168.1.129）的一次性 DNS 试验，
**不是** AuroraMihomo 开箱默认或文档推荐配置。

- 可能硬编码策略组名（如 `✈️ 国外`）
- 可能写入裸 UDP `8.8.8.8#组` 或域名 DoH，与当前产品防污染方案不一致

产品路径请用：面板 AdGuard「一键入口 DNS」+ 用户在 base 中自配
`https://8.8.8.8/dns-query#<你的境外策略组>`（组名因订阅而异）。
