# Binaries
#
# 这两条必须用 / 锚定在仓库根：裸写 `api` 会匹配任意层级的同名路径，
# 把整个 backend/api/ 源码目录（含 handler、logic、svc 与 etc/aurora-api.yaml）
# 一并排除掉——曾因此导致 136 个源文件从未入库，干净检出根本编译不了。
# 需要忽略的只是 `go build` 落在根目录的产物。
*.exe
*.exe~
*.dll
*.so
*.dylib
/api
/auroramihomo

# Data & runtime
/data/
*.db
*.db-journal
*.log

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.eslintcache

# 根 public/ 是前端构建产物（由 make build-frontend 生成）：
# 单二进制方案下前端资源已内嵌进二进制（见 backend/api/public 规则），
# 这里只作为运行时可覆盖的磁盘副本，不再入库——每次构建都会产生噪音 diff。
# 必须用 / 锚定根目录：裸写 public/ 会连 go:embed 的嵌入源
# backend/api/public 一起忽略，那条豁免规则（!backend/api/public/.gitkeep）
# 也会因父目录被忽略而失效。
/public/

# go:embed 内嵌源：make build-frontend 把 frontend/dist 拷进
# backend/api/public（二进制内嵌静态资源），属于构建产物，不入库；
# .gitkeep 用于保证 embed 目录存在（已跟踪，不受本规则影响）
backend/api/public/*
!backend/api/public/.gitkeep

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# AI 编码代理的会话产物。按 session id 命名、随每次会话累积，
# 对仓库使用者没有价值，也不构成项目文档。
.zcode/

# Claude Code 本机覆盖（权限审批、本机记忆）；团队共享配置在 .claude/settings.json
.claude/settings.local.json
CLAUDE.local.md

# Claude skills 是指向 .agents/skills/ 的本机联接（见 scripts/setup-claude-desktop.sh）
# 只提交 .claude/skills/README.md，避免把源目录再复制进 git
.claude/skills/*
!.claude/skills/README.md

# Go
vendor/
*.test
*.out
coverage.*

# Local env
.env
.env.*

# git worktrees (local isolated checkouts)
.worktrees/

# 临时诊断/部署脚本与一次性补丁（会话产物，不随发版）
/tmp_*
scripts/patch_129_*.py

# 构建产物与备份包
*.tar.gz
*.tgz
/auroramihomo-linux-amd64
/bin/
/node_modules/

# 本机 AI 会话状态
.superpowers/

# Python 缓存
__pycache__/
